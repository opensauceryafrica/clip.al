# Hoist nothing implicitly — keep dependency boundaries honest across the workspace.
# Packages must declare what they import.
auto-install-peers=true
strict-peer-dependencies=false
link-workspace-packages=true
prefer-workspace-packages=true
# Native deps (argon2) ship prebuilt binaries; allow their install scripts.
enable-pre-post-scripts=true
